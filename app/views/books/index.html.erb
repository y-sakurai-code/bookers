<!DOCTYPE HTML>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>Bookers</title>
    <link rel="stylesheet" href="/app/assets/stylesheets/books.scss" />
  </head>

  <body>
    <div class="box">
    <h1 class="title">Books</h1>
      <table class="table">
        <thead>
          <tr>
            <th class="title_tag">Title</th>
            <th class="title_tag">Body</th>
          </tr>

          <% @books.each do |book| %>
          <tr>
            <th class="book_title"><%= book.title %></th>
            <th class="book_body"><%= book.body %></th>
            <th class="book_menu"><%= link_to "Show", book_path(book.id) %></th>
            <th class="book_menu"><%= link_to "Edit", edit_book_path(book.id) %></th>
            <th class="book_menu"><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure ?" %></th>
          </tr>
          <% end %>
        </thead>
      </table>

      <br>

      <div class="errors">
      <% if @book && @book.errors.any? %>
        <%= @book.errors.count %>件のエラーが発生しました
          <ul>
            <% @book.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
      <% end %>
      </div>
    <div class="create_manu">
    <h2 class="new_book">New book</h2>

      <%= form_with model: @book do |f| %>
        <h5 class="new_title">Title</h5>
          <%= f.text_field :title %>
	
        <h5 class="new_body">Body</h5>
          <%= f.text_area :body %>

          <%= f.submit 'Create Book' %>

      <% end %>
      </div>
   </div>
  </body>
</html>
